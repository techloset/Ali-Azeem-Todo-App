import {View, Text} from 'react-native';
import React from 'react';

const Header = () => {
  return (
    <View style={{backgroundColor: 'red', padding: 10}}>
      <Text style={{fontSize: 40, textAlign: 'right'}}>0</Text>
    </View>
  );
};

export default Header;
